### Project Setup

```bash
# clone project from git url
$ git clone URL
$ cd project-folder

# Setup .env file Database and Mail Params

$ php artisan migrate

# install dependencies
$ npm install
$ npm run dev
$ php artisan migrate
```
